cmake_minimum_required(VERSION 3.8)

add_executable(lsr main.cpp image.h image.cpp)
target_link_libraries(lsr PRIVATE fmt)
# Notice: STB is header only and is added by Conan to includes automatically
